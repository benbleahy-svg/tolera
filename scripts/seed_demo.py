"""``python -m scripts.seed_demo`` — org-provisioning seed.

M0.1 only wires the command referenced by CLAUDE.md's run-commands. The
idempotent seed runner + two orgs (including the ``fechner`` pilot) land in M0.5.
"""

from __future__ import annotations


def main() -> None:
    print("seed_demo: no seed data yet — the org-provisioning seed lands in M0.5.")


if __name__ == "__main__":
    main()
