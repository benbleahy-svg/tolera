# Paperless Parts KB — drop zone (TIER 5, descriptive)

**This folder is an empty placeholder.** The Paperless Parts knowledge base (186 Markdown articles) is the upstream reference the sub-specs were distilled from. It is **not yet on disk**.

## Action (Benjamin)

Unzip the KB here so it's locally greppable. After unzipping, this folder should contain:

```
docs/reference/kb/
├── INDEX.md
├── index.html
├── 01-… / 02-… / … / 14-…   (topic folders)
└── (186 article .md files)
```

## How to treat it

- **Tier 5 — descriptive only.** It documents "how Paperless Parts does it," not what Tolera must do.
- It is **superseded** by tiers 1–3 wherever they speak, and **always** by `docs/subsystems/DACH-DELTA-LAYER.md`.
- When tracing a requirement, cite the specific article by slug (e.g. `building-review-rules`).

See `CLAUDE.md` §4 for the full description of this source.
